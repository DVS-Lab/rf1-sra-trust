# Historical code

These files document older SRNDNA/RF1 analyses but are not supported production entry points. Current commands are listed in `code/README.md`. Archived code may contain obsolete paths and dependencies by design.
