# Generated derivatives

`derivatives/fsl/` contains generated three-column EVs, rendered FEAT designs, L1 `.feat`, L2 `.gfeat`, physiological series, and resampled analysis masks. These outputs are intentionally ignored by Git. Only this README and `.gitkeep` are tracked.
