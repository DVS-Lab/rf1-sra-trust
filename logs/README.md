# Logs

`code/run_logged.sh` writes high-volume raw logs under ignored `logs/runs/` and compact Markdown summaries under tracked `logs/records/`. Runlists under `logs/runlists/` are ignored by default except the directory placeholder; commit a scientifically important runlist intentionally if needed.
