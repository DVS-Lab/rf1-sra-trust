# Historical templates

These templates are retained for provenance only. They are excluded from the active `model-1` contract and routine validation. L3 science was intentionally not modernized in this cleanup.
